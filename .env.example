# Databricks Workspace Configuration
# Copy this file to .env and fill in your actual credentials
# .env is excluded from git for security - never commit credentials!

# Databricks Host (workspace URL)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Databricks Personal Access Token (optional, if not using service principal)
# DATABRICKS_TOKEN=your_personal_access_token_here

# Service Principal OAuth M2M credentials
DATABRICKS_CLIENT_ID=your_service_principal_client_id
DATABRICKS_CLIENT_SECRET=your_service_principal_client_secret

# Azure AD tenant ID (Directory ID from Azure Portal → Switch Directory)
# Stored in de-scope Databricks secret but not used in _params.py (kept for reference)
AZURE_TENANT_ID=your_azure_tenant_id

# Optional: Databricks workspace ID
# DATABRICKS_WORKSPACE_ID=your_workspace_id
